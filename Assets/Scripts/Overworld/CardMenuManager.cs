using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CardMenuManager : MonoBehaviour
{
    MenuCardSlot[] yuaCards  = new MenuCardSlot[5];
    MenuCardSlot[] loganCards = new MenuCardSlot[5];
    MenuCardSlot[] danCards = new MenuCardSlot[5];
    MenuCardSlot[] jimCards = new MenuCardSlot[5];

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
